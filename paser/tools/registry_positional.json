[
  [
    "read_file",
    "Read file content",
    {
      "path": "string"
    }
  ],
  [
    "write_file",
    "Write content to file. Returns 'OK' on success.",
    {
      "path": "string",
      "contenido": "string"
    }
  ],
  [
    "remove_file",
    "Delete file. Returns 'OK' on success. Throws ERR if not found.",
    {
      "path": "string"
    }
  ],
  [
    "list_dir",
    "List directory contents",
    {
      "path": "string"
    }
  ],
  [
    "replace_string",
    "Search and replace string. Returns 'OK' on success. If no exact match is found, it may suggest a similar line using fuzzy matching. Throws ERR if ambiguous or not found.",
    {
      "path": "string",
      "search_text": "string",
      "replace_text": "string"
    }
  ],
  [
    "analyze_pyright",
    "Run pyright analysis",
    {
      "path": "string"
    }
  ],
  [
    "search_text_global",
    "Search string in all files",
    {
      "query": "string"
    }
  ],
  [
    "search_files_pattern",
    "Search files by pattern",
    {
      "pattern": "string"
    }
  ],
  [
    "rename_path",
    "Move/Rename path. Returns 'OK' on success. Throws ERR if origin not found.",
    {
      "origen": "string",
      "destino": "string"
    }
  ],
  [
    "validate_json",
    "Validates if a string is a valid JSON",
    {
      "json_string": "string"
    }
  ],
  [
    "run_instance",
    "Launch a new interactive instance of paser-mini. Use 'args' for CLI flags (e.g. ['-si', '...']) and 'message' for the initial chat input.",
    {
      "message": "string",
      "args": "list"
    }
  ]
]