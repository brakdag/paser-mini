Primero que nada no escribas acá nunca en el chat tags xml a menos que vayas a llamar bien a una tool, no uses para ejem
plos tags xml en su lugar enciera con corchetes [TOOL_CALL], Lo que vamos a hacer es lo siguiente, actualmente el programa
responde a la petición de una tool en TOOL_RESPOND, como siel usuario contestara, pero queremos cambiar eso a que sea la
misma IA, la que se conteste a si misma. Asi se siente mas en contexto, podemos hacer esto?Primero que nada no escribas acá nunca en el chat tags xml a menos que vayas a llamar bien a una tool, no uses para ejem
plos tags xml en su lugar enciera con corchetes [TOOL_CALL], Lo que vamos a hacer es lo siguiente, actualmente el programa
responde a la petición de una tool en TOOL_RESPOND, como siel usuario contestara, pero queremos cambiar eso a que sea la
misma IA, la que se conteste a si misma. Asi se siente mas en contexto, podemos hacer esto?
