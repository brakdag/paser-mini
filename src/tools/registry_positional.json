[
  [
    "fs.readFile",
    "Read file content. Use 'tail' to read only the last N lines (bypasses size limit).",
    {
      "path": "string",
      "tail": "number"
    }
  ],
  [
    "fs.writeFile",
    "Write content to file. Returns 'OK' on success.",
    {
      "path": "string",
      "content": "string"
    }
  ],
  [
    "fs.rm",
    "Delete file. Returns 'OK' on success. Throws ERR if not found.",
    {
      "path": "string"
    }
  ],
  [
    "fs.readdir",
    "List directory contents",
    {
      "path": "string"
    }
  ],
  [
    "fs.replaceString",
    "Search and replace string. Returns 'OK' on success. If no exact match is found, it may suggest a similar line using fuzzy matching. Throws ERR if ambiguous or not found.",
    {
      "path": "string",
      "search_text": "string",
      "replace_text": "string"
    }
  ],

  [
    "pyright.analyze",
    "Run static analysis for JS/TS codebase to detect type and syntax errors",
    {
      "path": "string"
    }
  ],

  [
    "eslint.lint",
    "Run ESLint analysis",
    {
      "path": "string"
    }
  ],
  [
    "jsdoc.generate",
    "Generate HTML documentation using JSDoc",
    {
      "path": "string",
      "outputDir": "string"
    }
  ],
  [
    "child_process.exec",
    "Execute a bash command in the project root",
    {
      "command": "string"
    }
  ],
  [
    "grep.search",
    "Search string in all files",
    {
      "query": "string"
    }
  ],
  [
    "glob.search",
    "Search files by pattern",
    {
      "pattern": "string"
    }
  ],
  [
    "fs.rename",
    "Move/Rename path. Returns 'OK' on success. Throws ERR if origin not found.",
    {
      "origin": "string",
      "destination": "string"
    }
  ],
  [
    "fs.copyFile",
    "Copy file from origin to destination. Returns 'OK' on success.",
    {
      "origin": "string",
      "destination": "string"
    }
  ],
  [
    "json.validate",
    "Validates if a string is a valid JSON",
    {
      "json_string": "string"
    }
  ],
  [
    "config.setNickname",
    "Changes the agent's display nickname in the chat log",
    {
      "newNickname": "string"
    }
  ],

  [
    "memento.push",
    "Stores in memento.log. To reference MUST use the #ID format (e.g., #1).",
    "string"
  ],
  [
    "chatManager.getTokenCount",
    "Returns the current token count of the conversation history and its percentage relative to the limit.",
    {}
  ],
  ["git.lsFiles", "Get the project file tree (very fast)", {}],
  [
    "git.diff",
    "Show differences of a file using git diff",
    {
      "path": "string"
    }
  ],
  [
    "git.restore",
    "Restore a file to its previous state using git restore",
    {
      "path": "string"
    }
  ],

  [
    "fs.concatFile",
    "Concatenates the content of the source file to the end of the destination file.",
    {
      "destination": "string",
      "source": "string"
    }
  ],

  [
    "json.getStructure",
    "Returns the keys and structure of a JSON node at a given path.",
    {
      "file_path": "string",
      "path": "string"
    }
  ],
  [
    "json.getNode",
    "Retrieves the content of a specific JSON node using a path.",
    {
      "file_path": "string",
      "path": "string"
    }
  ],
  [
    "json.getArrayInfo",
    "Returns the length and item type of a JSON array at a given path.",
    {
      "file_path": "string",
      "path": "string"
    }
  ],
  [
    "json.updateNode",
    "Updates a specific JSON node with a new value using a path.",
    {
      "file_path": "string",
      "path": "string",
      "value": "any"
    }
  ],
  [
    "github.listIssues",
    "List issues in a GitHub repository",
    {
      "repo": "string"
    }
  ],
  [
    "github.createIssue",
    "Create a new GitHub issue",
    {
      "title": "string",
      "body": "string",
      "repo": "string"
    }
  ],
  [
    "github.editIssue",
    "Edit an existing issue in a GitHub repository",
    {
      "issue_number": "integer",
      "repo": "string",
      "title": "string",
      "body": "string"
    }
  ],
  [
    "github.closeIssue",
    "Close an issue in a GitHub repository",
    {
      "issue_number": "integer",
      "repo": "string"
    }
  ],
  [
    "github.postComment",
    "Post a comment to a specific GitHub issue.",
    {
      "issue_number": "integer",
      "body": "string"
    }
  ],
  ["git.remoteUrl", "Get the current GitHub repository name", {}],
  [
    "git.diffAll",
    "Show all changes in the current repository using git diff",
    {}
  ],
  [
    "system.notify",
    "Sends a notification to the user and plays a sound.",
    {
      "message": "string"
    }
  ],
  [
    "fountain.insertScene",
    "Insert a Fountain scene into the chat as a user message.",
    {
      "scene": "string",
      "action": "string"
    }
  ],
  [
    "jszip.listContents",
    "Lists all files contained within a ZIP archive without loading it into memory.",
    {
      "filePath": "string"
    }
  ],
  [
    "binary.analyze",
    "Perform binary analysis on a file (inspect, extract, search, detect, convert)",
    {
      "action": "string",
      "filePath": "string",
      "offset": "number",
      "length": "number",
      "end": "number",
      "endianness": "string"
    }
  ],
  [
    "duckduckgo.search",
    "Search the web using DuckDuckGo Lite.",
    {
      "query": "string"
    }
  ],
  [
    "elinks.render",
    "Render a webpage to text using elinks.",
    {
      "url": "string"
    }
  ],
  [
    "vm.runInContext",
    "Execute raw JavaScript in a secure, isolated V8 sandbox with BrowserFS access. Returns execution trace and result.",
    {
      "code": "string"
    }
  ],
  [
    "seeImage",
    "Visualizes an image. Supports optional cropping [left, top, right, bottom].",
    {
      "path": "string",
      "crop": "number[]"
    }
  ],
  [
    "ast.analyze",
    "Analyze JavaScript AST. Can filter by node type using 'query'.",
    {
      "path": "string",
      "query": "string"
    }
  ]
]
