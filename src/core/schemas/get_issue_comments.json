{
  "name": "get_issue_comments",
  "description": "Retrieves all comments for a specific GitHub issue to understand the conversation and feedback.",
  "parameters": {
    "type": "object",
    "properties": {
      "issue_number": {
        "type": "integer",
        "description": "The number of the GitHub issue to retrieve comments for."
      },
      "repo": {
        "type": "string",
        "description": "The GitHub repository in 'owner/repo' format. If omitted, the current repository is used."
      }
    },
    "required": ["issue_number"]
  }
}